# Fly.io MoQ relay — QUIC + plain HTTP cert on TCP/443

[log]
level = "info"

[server]
listen = "[::]:443"
tls.generate = ["thetextapp-moq.fly.dev", "localhost"]

[web.http]
listen = "[::]:443"

[auth.public]
subscribe = ["anon", "demo"]
publish = ["anon", "demo/viewer"]
